# Future Improvements

- Replace the derived card-back image if a separate official card-back source file is provided.
- Add persistent room snapshots for server restarts.
- Add optional spectator mode for friends watching a table.
- Add richer sound effects and a mute/volume control.
- Expand drag-and-drop replacement after more desktop playtesting.
- Add host controls for configurable starting player, timer length, and wrong-match penalty mode.
