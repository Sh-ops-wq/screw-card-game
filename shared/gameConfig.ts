/** Default table size when host taps "Fill with bots" (solo / small groups). */
export const BOT_PREFILL_TABLE_SIZE = 4;

/** Number of games played per match. Lowest cumulative points after all games wins. */
export const GAMES_PER_MATCH = 5;
