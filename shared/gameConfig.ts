/** Default table size when host taps "Fill with bots" (solo / small groups). */
export const BOT_PREFILL_TABLE_SIZE = 4;

/** First player to collect this many *round wins* wins the overall match (best-of style). */
export const MATCH_ROUND_WINS_TO_WIN = 5;
